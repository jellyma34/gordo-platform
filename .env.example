# Скопируйте в `.env.local` или задайте в Railway (Variables) для сервиса фронта.
# Обязательно абсолютный URL FastAPI (https://...), без завершающего /api.
NEXT_PUBLIC_API_URL=https://gordo-platform-dev.up.railway.app
NEXT_PUBLIC_APP_ENV=development

# NEXT_PUBLIC_DEBUG_API=1

# Production-сборка: см. `.env.production`.
