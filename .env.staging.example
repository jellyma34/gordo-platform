# URL бэкенда FastAPI для staging (не домен фронтенда). В Railway — переменная сервиса frontend при сборке.
NEXT_PUBLIC_API_URL=https://<your-staging-backend>.up.railway.app
NEXT_PUBLIC_APP_ENV=staging
