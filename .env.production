# Базовый URL бэкенда для production-сборки (`next build` / `next start`).
# В Railway для staging задайте отдельный сервис/переменную: URL именно API (FastAPI), не фронта.
NEXT_PUBLIC_API_URL=https://gordo-platform-production.up.railway.app
